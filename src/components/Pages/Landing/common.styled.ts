import { styled } from '@/styled-system/jsx';

export const FeaturedCarCarouselContainer = styled('div', {
  base: {
    overflow: 'hidden',
  },
});

export const FeaturedDealerCarouselContainer = styled('div', {
  base: {
    overflow: 'hidden',
    paddingY: 'lg',
  },
});

export const CorouselViewport = styled('div', {
  base: {},
});
